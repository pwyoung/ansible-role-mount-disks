---
- name: Show run status
  hosts: all
  tasks:
    - debug: msg="Inside ansible-role-mount-disks/test/playbook.yaml"

- name: Show params
  hosts: all
  tasks:
    - debug: msg="disk_pattern={{ disk_pattern }}"

- hosts: all
  roles:
    - role: ansible-role-mount-disks
